# agent-chrome-wrapper

Provides an MCP that gives agents access to Chrome, starting a separate Chrome instance for each session.

## Key features

<!-- One short paragraph or a few bullets a user can skim to decide whether this
     plugin fits their workflow. Replace this placeholder before cutting v0.0.1. -->

- TODO: per-session isolated Chrome instance — each agent session drives its own browser
- TODO: key capability two (e.g. navigation / DOM / screenshot tooling)
- TODO: pairs with agent-vdesktop — launched Chrome windows can be positioned/managed
